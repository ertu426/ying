---
interface Props {
  date: Date
}

const { date } = Astro.props;
---

<time class="mb-0.5 text-xs text-zinc-400" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('zh-cn', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
